<Window x:Class="LibraryTrackerApp.Views.Register"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="250" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="1" Margin="0,5,0,10" />

        <TextBlock Text="Password:" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,5,0,10"/>

        <Button Content="Register" Grid.Row="4" Width="100" HorizontalAlignment="Center" Click="RegisterButton_Click"/>
    </Grid>
</Window>
