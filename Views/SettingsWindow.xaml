<Window x:Class="LibraryTrackerApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library Settings" Height="200" Width="300">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Overdue Time (days):" Width="150" VerticalAlignment="Center"/>
            <TextBox x:Name="OverdueDaysTextBox" Width="50" PreviewTextInput="NumberOnly_PreviewTextInput"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Overdue Fee ($):" Width="150" VerticalAlignment="Center"/>
            <TextBox x:Name="OverdueFeeTextBox" Width="50" PreviewTextInput="DecimalOnly_PreviewTextInput"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Save Settings" Width="120" HorizontalAlignment="Center" Click="SaveSettings_Click"/>
    </Grid>
</Window>
